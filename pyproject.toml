[project]
name = "data-storage-bet"
version = "0.0.1"
requires-python = ">=3.12"
authors = [
    {name = "Palm"},
    {name = "Arm"},
    {name = "Drake"}
]
dependencies = [
    "psycopg>=3.3.5",
    "python-dotenv>=1.2.3",
    "sqlalchemy>=2.0.52",
]

[dependency-groups]
dev = [
    "psycopg[binary]>=3.3.5",
]
